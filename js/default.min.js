/**
 * jQuery Plugins
 *
 * A concatenate collection of jQuery plugins used thoughout the site.
 * Concatenating the plugins into one file reduces HTTP requests for the client.
 *
 * @package WordPress
 * @subpackage DPE_Baseline
 *//* **********************************************
     Begin default.js
********************************************** *//**
 * Sitewide Scripts
 *
 * JavaScripts used throughout the public site.
 *
 * @package WordPress
 * @subpackage DPE_Baseline
 */jQuery(document).ready(function(e){e("#menu-toggle").click(function(t){t.preventDefault();e("#site-nav").slideToggle()})});